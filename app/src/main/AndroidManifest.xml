<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jvd.ir.digiknew">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".Basket.BasketActivity"></activity>
        <activity android:name=".EditProfile.EditingProfile" />
        <activity android:name=".EditProfile.EditProfile" />
        <activity android:name=".Filter.FilterActivity" />
        <activity android:name=".Cat.CatActivity" />
        <activity android:name=".Chart.ChartActivity" />
        <activity android:name=".CompareProduct.CompareActivity" />
        <activity android:name=".Comments.AddComment.AddCommentActivity" />
        <activity
            android:name=".Comments.CommentAvitivity"
            android:label="@string/title_activity_comment_avitivity" />
        <activity android:name=".Properties.PropertiesActivity" />
        <activity android:name=".Detail.DetailActivity" />
        <activity
            android:name=".SignUp.SignUpActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Login.LoginActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>